{
  "name": "feedforwardclub",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/sync-calendar-to-public.mjs && astro dev",
    "start": "node scripts/sync-calendar-to-public.mjs && astro dev",
    "build": "node scripts/sync-calendar-to-public.mjs && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && wrangler deploy",
    "worker:preview": "npm run build && wrangler dev",
    "calendar:push": "node scripts/publish-calendar-kv.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^4.16.0",
    "cobe": "^2.0.1",
    "sass": "^1.99.0"
  },
  "devDependencies": {
    "@astrojs/cloudflare": "^11.2.0",
    "wrangler": "^4.20.0"
  }
}
